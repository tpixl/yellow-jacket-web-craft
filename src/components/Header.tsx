
import React from "react";

const Header: React.FC = () => {
  return (
    <header className="fixed top-0 left-0 w-full bg-dodge-black bg-opacity-80 backdrop-blur-md z-50">
     
    </header>
  );
};

export default Header;
